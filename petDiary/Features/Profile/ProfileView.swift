import SwiftUI

struct ProfileView: View {
    var body: some View {
        ZStack {
            Color.brandBackgroundLight.ignoresSafeArea()
            VStack {
                Text("Profile")
            }
        }
    }
}
