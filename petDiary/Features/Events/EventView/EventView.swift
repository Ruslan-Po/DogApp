import SwiftUI

struct EventListView: View {
    var body: some View {
        ZStack {
            Color.brandBackgroundLight.ignoresSafeArea()
            VStack {
                Text("EventList")
            }
        }
    }
}


