import SwiftUI

struct PetView: View {
    var body: some View {
        ZStack {
            Color.brandBackgroundLight.ignoresSafeArea()
            VStack {
                Text("Pet View")
            }
        }
    }
}
